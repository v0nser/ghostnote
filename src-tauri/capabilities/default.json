{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal surface for the GhostNote main window. Window geometry is driven from Rust, so the frontend only gets the few window verbs it genuinely needs for custom chrome.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-is-minimized"
  ]
}
